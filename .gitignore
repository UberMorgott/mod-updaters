# Build artefacts
*.bak
*.bak*
*.tmp

# Build-time secrets (server address + credentials) — NEVER commit
config.txt

# Editor local state
.vscode/
.idea/

# Go build cache
*.test
*.out

# NOTE: uar.exe and wur.exe are NOT ignored — they are the shipped payload.
